{% extends 'base.html.twig' %}

{% block title %}Prediction rounds{% endblock %}

{% block menu %}
   {{ include('_nav_menu.html.twig') }}
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">{{ competitionName }}</h3>
            <p class="card-text">Prediction rounds.</p>
        </div>
        <div class="card-body">
            <ul class="list-group list-group-flush">
                {% for round in rounds %}
                    <li class="list-group-item">
                        <a class="card-link prediction-rounds" href="{{ path('app_prediction_round', {'id': competitionId, 'round': round.name}) }}">
                            <span>
                                <span class="circle circle-{{ round.status|lower }}"><span class="status-{{ round.status|lower }}"></span></span>
                                {% if round.name matches '/^\\d+$/' %}
                                    {{ 'Round #' ~ round.name}}
                                {% else %}
                                    {{ round.name|replace({'_': " "})|lower|capitalize }}
                                {% endif %}
                            </span>
                            <span>{{ round.dateFrom|date('d.m') ~ ' - ' ~ round.dateTo|date('d.m') }}</span>
                            <span>{{ round.matches }}</span>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>

{% endblock %}
